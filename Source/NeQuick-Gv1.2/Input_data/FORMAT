Input files for different stations distributed around the world for doy 100 of years 2000 (high solar activity), 2004 (medium solar activity) 
and 2007 (low solar activity)

Format:
*Filename: 
	<StationCode>_in.dat

contents:

month Time(UTC hours)  lon(sta) lat(sta) heigth(sta) lon(sat) lat(sat) heigth(sat)

with lat, lon in degrees, height in meters and assuming WGS-84 ellipsoidal coordinates: geodetic latitude, 
geodetic longitude and ellipsoidal height.

The file Az_used.dat contains the parameters used for given Solar Flux.